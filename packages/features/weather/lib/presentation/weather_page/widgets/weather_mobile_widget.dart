import 'package:flutter/material.dart';

class WeatherMobileWidget extends StatelessWidget {
  const WeatherMobileWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
