import 'package:flutter/material.dart';

class ProjectListMobileWidget extends StatelessWidget {
  const ProjectListMobileWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
