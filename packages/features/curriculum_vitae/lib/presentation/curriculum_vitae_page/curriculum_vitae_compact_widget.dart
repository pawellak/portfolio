import 'package:flutter/material.dart' show Column, StatelessWidget, Widget;

class CurriculumVitaeCompactWidget extends StatelessWidget {
  const CurriculumVitaeCompactWidget({super.key});

  @override
  Widget build(_) => const Column();
}
